server {
    listen 80 default_server;
    server_name 91.98.64.49;
    server_tokens off;
    root /var/www/dead-frequency;
    location /.well-known/acme-challenge/ { try_files $uri =404; }
    location / { return 503; }
}
